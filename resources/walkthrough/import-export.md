## Back Up & Migrate Your Data

Use the **`...`** menu in the **Editor Pockets** view title bar to access:

- **Export Data to JSON…** — save all pockets to a JSON file for backup or sharing.
- **Import Data from JSON…** — restore pockets from a previously exported file.

This makes it easy to move your pockets between machines or recover them after a reinstall.
